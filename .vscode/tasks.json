{
    "tasks": [
        {
            "type": "shell",
            "label": "conan",
            "command": "/bin/bash",
            "args": [
                "${workspaceFolder}/.vscode/conan.sh",
                "${workspaceFolder}",
                "${command:cmake.buildType}"
            ],
            "problemMatcher": [
                "$gcc"
            ],
            "group": "build"
        }
    ],
    "version": "2.0.0"
}